{
  "name": "@cauldron/players",
  "description": "A library that allows caching of player history",
  "main": "./dist/index.js",
  "scripts": {
    "compile": "babel src --out-dir dist --copy-files"
  }
}
